{
  "name": "@merkle-news/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @merkle-news/subgraph auth",
    "subgraph:codegen": "yarn workspace @merkle-news/subgraph codegen",
    "subgraph:build": "yarn workspace @merkle-news/subgraph build",
    "subgraph:deploy": "yarn workspace @merkle-news/subgraph deploy",
    "react-app:build": "yarn workspace @merkle-news/react-app build",
    "react-app:eject": "yarn workspace @merkle-news/react-app eject",
    "react-app:ipfs": "yarn workspace @merkle-news/react-app ipfs",
    "react-app:start": "yarn workspace @merkle-news/react-app start",
    "react-app:test": "yarn workspace @merkle-news/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
